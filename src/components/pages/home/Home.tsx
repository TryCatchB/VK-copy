import { Button } from "@mui/material";
import { FC } from "react";

const Home: FC = () => {
  return (
    <div>
      <Button variant="contained">Hello World</Button>
    </div>
  );
};

export default Home;
